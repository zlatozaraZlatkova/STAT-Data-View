import { ExtractWordPipe } from './extract-word.pipe';

describe('ExtractWordPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractWordPipe();
    expect(pipe).toBeTruthy();
  });
});
